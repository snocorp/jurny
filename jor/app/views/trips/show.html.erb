<h1><%= @trip.name %></h1>
<small><%= link_to @trip.owner.fullname, traveller_path(@trip.owner) %></small>

<p><%= @trip.summary %></p>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
